{% extends 'layout.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block content %}
    <article class="max-w-4xl mx-auto px-4 py-8">
        <header class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900 mb-2">{{ post.title }}</h1>
            <div class="flex items-center text-sm text-gray-500">
                <span>Published on {{ post.publishedAt|date('F j, Y') }}</span>
                <span class="mx-2">•</span>
                <span>5 min read</span>
            </div>
            {% if post.featuredImage %}
                <img src="{{ post.featuredImage }}" alt="{{ post.title }}" class="mt-6 rounded-lg w-full object-cover h-64">
            {% endif %}
        </header>

        <div class="prose max-w-none">
            {{ post.content|raw }}
        </div>

        {# {% if is_granted('ROLE_ADMIN') %}
            <div class="mt-8 pt-6 border-t border-gray-200">
                <a href="{{ path('admin_blog_edit', {'id': post.id}) }}" class="text-blue-600 hover:text-blue-800">Edit Post</a>
            </div>
        {% endif %} #}
    </article>
{% endblock %}