<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>
      {% block title %}
        {{ APP_NAME }}
      {% endblock %}| Wilson Wanja - Full-Stack Developer
    </title>

    <meta name="description"
      content="{% block description %}
      {% endblock %}" />
    <meta name="author" content="Wilson Wanja" />
    <meta name="keywords" content="Wilson Wanja, Full-stack Developer, Slim PHP, Flask, Linux, Portfolio" />

    <!-- Open Graph (OG) Tags for social media -->
    <meta property="og:title"
      content="{% block og_title %}
      {% endblock %}" />
    <meta property="og:description"
      content="{% block og_description %}
      {% endblock %}" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="{{ APP_URL|default('https://wilson.4ly.me') }}" />
    <meta property="og:image" content="{{ APP_URL|default('https://example.com') }}/assets/og-image.jpg" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title"
      content="{% block twitter_title %}
      {% endblock %}" />
    <meta name="twitter:description"
      content="{% block twitter_description %}
      {% endblock %}" />
    <meta name="twitter:image" content="{{ APP_URL|default('https://example.com') }}/assets/twitter-card.jpg" />

    <!-- Tailwind CSS -->
    {% if APP_ENVIRONMENT != 'DEV' %}
      {# Development - use Vite server #}

      <script type="module" src="http://localhost:5173/@vite/client"></script>
      <script type="module" src="http://localhost:5173/resources/js/app.js"></script>
      <link rel="stylesheet" href="http://localhost:5173/resources/css/app.css" />
    {% else %}
      {# Production - use built assets #}
      <link rel="stylesheet" href="{{ asset('/build/assets/app.D65zor3x.css') }}" />
      <script type="module" src="{{ asset('/build/assets/app-C6K0LEQp.js') }}"></script>
    {% endif %}

    <!-- Favicon -->
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
  </head>
  <body class="bg-gray-100 dark:bg-gray-700 text-gray-900 dark:text-gray-100 font-sans">
    {% include 'partials/header.twig' %}
    <main>
      {% block content %}

      {% endblock %}
    </main>
    {% include 'partials/footer.twig' %}
  </body>
</html>
